extends: configs/eval/base_external.yaml

experiment:
  id: grpo_external_v1
  checkpoint_stage: grpo

model:
  adapter_path: outputs/grpo_qlora_48g_domain_v1/final_adapter

training:
  output_dir: outputs/evaluation/grpo_external_v1
