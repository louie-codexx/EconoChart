extends: configs/eval/base_external.yaml

experiment:
  id: sft_external_v1
  checkpoint_stage: sft

model:
  adapter_path: outputs/sft_qlora_r16_domain_v1/final_adapter

training:
  output_dir: outputs/evaluation/sft_external_v1
