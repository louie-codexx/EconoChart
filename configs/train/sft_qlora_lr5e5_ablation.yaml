extends: configs/train/sft_qlora_4090.yaml

experiment:
  id: sft_qlora_lr5e5_ablation
  hypothesis: A lower adapter learning rate may preserve generic ChartQA ability at the cost of slower domain adaptation.

data:
  train:
    - path: data/generated/econochart_v2/subsets/sft_screen_4800.jsonl
      expected_rows: 4800

training:
  output_dir: outputs/screen/sft_qlora_r16_lr5e5
  num_train_epochs: 1
  learning_rate: 0.00005
