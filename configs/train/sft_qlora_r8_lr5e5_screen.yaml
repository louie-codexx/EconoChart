extends: configs/train/sft_qlora_r8_ablation.yaml

experiment:
  id: sft_qlora_r8_lr5e5_screen
  hypothesis: At the selected 5e-5 learning rate, rank 8 may match rank 16 with half the adapter capacity.

training:
  output_dir: outputs/screen/sft_qlora_r8_lr5e5
  learning_rate: 0.00005
